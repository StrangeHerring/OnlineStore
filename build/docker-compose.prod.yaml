ervices:
  db:
    image: postgres:16-alpine
    container_name: online_store_postgres
    env_file:
      - /etc/coursebackend/.env
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./init:/docker-entrypoint-initdb.d
    networks:
      - store_net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB" ]
      interval: 10s
      timeout: 3s
      retries: 3

  app:
    build:
      context: ..
      dockerfile: build/Dockerfile.prod
    container_name: online_store_app
    env_file:
      - /etc/onlinestore/.env
    volumes:
      - type: bind
        source: /etc/onlinestore/config.yaml
        target: /config/config.yaml
        read_only: true
        bind:
          create_host_path: false
    ports:
      - "8080:8080"
    networks:
      - store_net
    depends_on:
      db:
        condition: service_healthy

volumes:
  db_data:

networks:
  course_net:
    driver: bridge